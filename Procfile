# Railway Procfile
# Defines services to run in Railway

# Web API - Public endpoint for downloading signals and viewing stats
web: python api.py

# Collector - Background service to collect Telegram signals (optional, can run separately)
# collector: python main.py --mode collector

